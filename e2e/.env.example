# E2E Test Configuration
# Copy this to .env and fill in your values

# Base URL for E2E tests (defaults to http://localhost:3000 if not set)
PLAYWRIGHT_BASE_URL=http://localhost:3000

# GitHub test account credentials for E2E authentication (REQUIRED)
# IMPORTANT: If password contains special characters like #, wrap it in quotes
PLAYWRIGHT_GITHUB_USERNAME=your_test_account_email@example.com
PLAYWRIGHT_GITHUB_PASSWORD="your_test_account_password"

# Optional: Run browser in headed mode (see browser window)
# PLAYWRIGHT_HEADED=true
